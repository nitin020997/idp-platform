# Local kubeconfig / secrets that should never be committed
kubeconfig
*.kubeconfig
.env
*.local

# Terraform (if/when a cloud module is added later)
.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfvars

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp
